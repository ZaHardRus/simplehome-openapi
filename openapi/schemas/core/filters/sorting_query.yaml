in: query
name: sort
required: false
description: 'Принимает имя поля, по которому необходимо выполнить сортировку.
  Для изменения порядка сортировки, перед именем поля вставляется символ "-".'
schema:
  type: string
  example: -id